<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ymjtt.manager.mapper.ProductCatMapper">

    <resultMap id="productCatDoMap" type="productCatDo" >
        <id property="productCatId" column="product_cat_id" />
        <result property="productCatName" column="product_cat_name" />
        <result property="productCatStatus" column="product_cat_status" />
        <result property="sortOrder" column="sort_order" />
        <result property="parentId" column="parent_id" />
        <result property="parentCat" column="parent_cat" />
        <result property="createTime" column="createTime" />
        <result property="lastupdateTime" column="lastupdateTime" />
        <result property="updateCounts" column="update_counts" />
    </resultMap>

    <sql id="productCatCloumn">
        pc.product_cat_id, pc.product_cat_name, pc.product_cat_status, pc.sort_order, pc.parent_id, pc.parent_cat,
        pc.createTime, pc.lastupdateTime, pc.update_counts
    </sql>

    <!-- 父ID查询 -->
    <sql id="pidWhere">
        WHERE 1 = 1
        <if test="pid != null">
            AND pc.parent_id = #{pid}
        </if>
        <if test="productCatName != null">
            AND pc.product_cat_name = #{productCatName}
        </if>
    </sql>
    <select id="listByPid" parameterType="Long" resultMap="productCatDoMap">
        SELECT
        <include refid="productCatCloumn"/>
        FROM product_cat pc
        <include refid="pidWhere" />
        ORDER BY pc.sort_order DESC
    </select>

    <!-- ID查询 -->
    <sql id="idWhere">
        WHERE 1 = 1
        <if test="_parameter != null">
            AND pc.product_cat_id = #{id}
        </if>
    </sql>
    <select id="getById" parameterType="Long" resultMap="productCatDoMap">
        SELECT
        <include refid="productCatCloumn"/>
        FROM product_cat pc
        <include refid="idWhere" />
        ORDER BY pc.sort_order DESC
    </select>

    <!-- Name查询 -->
    <sql id="nameWhere">
        WHERE 1 = 1
        <if test="productCatName != null">
            AND pc.product_cat_name = #{productCatName}
        </if>
    </sql>
    <select id="getByName" parameterType="String" resultMap="productCatDoMap">
        SELECT
        <include refid="productCatCloumn"/>
        FROM product_cat pc
        <include refid="nameWhere" />
        ORDER BY pc.sort_order DESC
    </select>

    <!-- 更新 -->
    <update id="update" parameterType="productCatDo">
        UPDATE product_cat SET
        <if test="productCatId != null and productCatId != ''">
            product_cat_id = #{productCatId},
        </if>
        <if test="productCatName != null and productCatName != ''">
            product_cat_name = #{productCatName},
        </if>
        <if test="productCatStatus != null and productCatStatus != ''">
            product_cat_status = #{productCatStatus},
        </if>
        <if test="sortOrder != null and sortOrder != ''">
            sort_order = #{sortOrder},
        </if>
        <if test="parentId != null and parentId != ''">
            parent_id = #{parentId},
        </if>
        <!--<if test="parentCat != null and parentCat != ''">-->
            parent_cat = #{parentCat},
        <!--</if>-->
        <if test="1 != 2">
            createTime = createTime,
        </if>
        <if test="1 != 2">
            update_counts = update_counts + 1,
        </if>
        <if test="1 != 2">
            lastupdateTime = timestamp(now())
        </if>
        WHERE
        product_cat_id = #{productCatId}
    </update>

</mapper>