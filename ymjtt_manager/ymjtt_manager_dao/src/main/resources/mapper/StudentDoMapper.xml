<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ywx.ymjtt.dao.test.StudentDoMapper">

    <!-- 指定二级缓存实现类 -->
    <cache type="org.apache.ibatis.cache.impl.PerpetualCache" />

    <!-- sql片段 -->
    <sql id="studentCloum">stu.id, stu.name, stu.age</sql>
    <sql id="studentWhere">
        WHERE 1 = 1
        <if test="id != null">
            AND stu.id = #{id}
        </if>
        <if test="name != null">
            AND stu.name = #{name}
        </if>
    </sql>

    <!-- 查询 -->
    <select id="query" parameterType="StudentDo" resultType="StudentDo">
        SELECT
        <include refid="studentCloum"/>
        FROM student stu
        <include refid="studentWhere"/>
    </select>

    <!-- 增加 -->
    <insert id="add" parameterType="StudentDo">
        INSERT INTO student VALUES (null, #{name}, #{age})
    </insert>

    <!-- 删除 -->
    <delete id="delete" parameterType="StudentDo">
        DELETE FROM student
        <if test="id != null">
            WHERE id = #{id}
        </if>
    </delete>

    <!-- 更新 -->
    <update id="update" parameterType="StudentDo">
        UPDATE student stu
        <trim prefix="set" suffixOverrides=",">
            <if test="name != null">stu.name = #{name},</if>
            <if test="age != null">stu.age = #{age}</if>
        </trim>
        WHERE stu.id = #{id}
    </update>

</mapper>